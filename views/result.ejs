<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Result</title>
</head>
<body>
  <h1><%= action === 'analyze' ? 'Sentiment Analysis Result' : 'Summary' %></h1>

  <% if (error) { %>
    <p><%= error %></p>
  <% } else if (action === 'analyze') { %>
    <p>Text: <%= text %></p>
    <p>Sentiment: <%= result.sentiment %></p>
    <p>Score: <%= result.score %></p>
    <p>Confidence: <%= result.confidence %></p>
  <% } else if (action === 'summarize') { %>
    <p>Original Text: <%= text %></p>
    <p>Summary: <%= result %></p>
  <% } %>

  <h1>Vectorization Result</h1>

  <% if (error) { %>
    <p><%= error %></p>
  <% } else { %>
    <p>Text: <%= text %></p>
    <h2>Embedding Vector:</h2>
    <pre><%= JSON.stringify(vector, null, 2) %></pre>
  <% } %>

  <a href="/">Go back</a>
</body>
</html>